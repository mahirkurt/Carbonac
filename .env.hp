# Carbonac — HP Thin Client overrides
# Usage: docker compose --env-file .env --env-file .env.hp --profile hp-worker up -d

# Redis (connect to Pi over Tailscale — no local Redis needed)
REDIS_URL=redis://100.125.78.2:6379
REDIS_MEMORY_LIMIT=256m

# API (if running locally on HP)
API_HOST_PORT=3001
API_MEMORY_LIMIT=512m

# Worker (HP has more compute resources)
WORKER_MEMORY_LIMIT=2g
WORKER_CONCURRENCY=1
PDF_QA_ENABLED=false
